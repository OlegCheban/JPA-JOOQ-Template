spring:
  application:
    name: demo

  jpa:
    hibernate.ddl-auto: validate
    open-in-view: false
    show-sql: true

  datasource:
    hikari:
      auto-commit: false

  liquibase:
    liquibase-schema: public
    enabled: true